//método .slice() para dividir arrays.
//---.slice(inicio, fim);

//método .splice() para alterar arrays.
//---.splice(inicio, qtd a ser deletada a partir do inicio, oq vai ser add no lugar do que foi retirado);